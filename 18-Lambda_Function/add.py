# Definition:
# A lambda function is a small, anonymous function defined using the lambda keyword. It can take any number of arguments but has a single expression.

# Example:
# Lambda function to add two numbers:

add = lambda x, y: x + y  

print(add(3, 5))  # output -> 8

"""
add ---> is the name of the lambda function
lambda ---> keyword 
x, y ---> variables by using which we can take input 
x + y ---> this is expression 

we use print function to print the output in the add() ---> we can take any two numbers to add them and the final addition will print.

"""
"""
subtraction 
multiplication
division
"""